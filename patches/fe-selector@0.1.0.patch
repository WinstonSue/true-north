diff --git a/dist/core.cjs.js b/dist/core.cjs.js
index d84631cb46f59edec2239da9a57668ff5cbccee5..a8f44cd57e0719dfbadd1fb4146b6f60b1398c84 100644
--- a/dist/core.cjs.js
+++ b/dist/core.cjs.js
@@ -232,6 +232,21 @@ function extractElementContext(el) {
     boundingRect: { top: rect.top, left: rect.left, width: rect.width, height: rect.height }
   };
 }
+let finderModule = null;
+async function loadFinder() {
+  if (!finderModule) {
+    finderModule = await Promise.resolve().then(() => require("./finder-LZycm0fF.cjs"));
+  }
+  return finderModule;
+}
+async function generateCssSelector(el) {
+  try {
+    const { finder } = await loadFinder();
+    return finder(el, { root: document.documentElement });
+  } catch {
+    return void 0;
+  }
+}
 const IGNORED_TAGS = /* @__PURE__ */ new Set(["HTML", "BODY", "HEAD", "SCRIPT", "STYLE", "LINK", "META"]);
 class Selector {
   constructor(options, transport) {
@@ -275,7 +290,7 @@ class Selector {
       if (this.transport.isSandboxMode) {
         this.transport.sendElementSelected(ctx);
       } else if (!this.options.silent) {
-        this.options.onSelect(ctx);
+        this.options.onSelect(ctx, el);
       }
     };
     this.onKeyDown = (e) => {
@@ -304,7 +319,8 @@ class Selector {
       activationKey: options.activationKey ?? "alt",
       onSelect: options.onSelect ?? (() => {
       }),
-      silent: options.silent ?? false
+      silent: options.silent ?? false,
+      isSelectable: options.isSelectable ?? (() => true)
     };
     if (this.options.activationKey === "none") {
       this.alwaysOn = true;
@@ -365,14 +381,19 @@ class Selector {
     this.alwaysOn = active;
     if (!active) {
       this.hovered = null;
-      this.selected = null;
-      this.overlay.hide();
-      this.transport.sendDeselect();
+      if (!this.selected)
+        this.overlay.hide();
     }
   }
+  clearSelected() {
+    this.hovered = null;
+    this.selected = null;
+    this.overlay.hide();
+    this.transport.sendDeselect();
+  }
   getTarget(e) {
     const el = e.target;
-    if (!el || IGNORED_TAGS.has(el.tagName))
+    if (!el || IGNORED_TAGS.has(el.tagName) || !this.options.isSelectable(el))
       return null;
     return el;
   }
@@ -462,12 +483,18 @@ class Transport {
 }
 let instance = null;
 function init(options = {}) {
-  if (instance)
-    return;
-  const transport = new Transport();
-  const selector = new Selector(options, transport);
-  instance = { selector, transport };
-  selector.init();
+  if (!instance) {
+    const transport = new Transport();
+    const selector = new Selector(options, transport);
+    instance = { selector, transport };
+    selector.init();
+  }
+  return {
+    activate: () => instance == null ? void 0 : instance.selector.setActive(true),
+    deactivate: () => instance == null ? void 0 : instance.selector.setActive(false),
+    clearSelected: () => instance == null ? void 0 : instance.selector.clearSelected(),
+    destroy
+  };
 }
 function destroy() {
   if (!instance)
@@ -479,5 +506,6 @@ function destroy() {
 exports.Transport = Transport;
 exports.destroy = destroy;
 exports.extractElementContext = extractElementContext;
+exports.generateCssSelector = generateCssSelector;
 exports.init = init;
 //# sourceMappingURL=core.cjs.js.map
diff --git a/dist/core.d.ts b/dist/core.d.ts
index 63b206123c78d0da6adb98b343f9d406f99e9100..836d9f9973fb4c1b09e0b8d9745d74f87811bca1 100644
--- a/dist/core.d.ts
+++ b/dist/core.d.ts
@@ -19,9 +19,18 @@ export declare interface ElementContext {
         width: number;
         height: number;
     };
+    cssSelector?: string;
 }
 
 export declare function extractElementContext(el: Element): ElementContext;
+export declare function generateCssSelector(el: Element): Promise<string | undefined>;
+
+export declare interface ElementSelectorController {
+    activate(): void;
+    deactivate(): void;
+    clearSelected(): void;
+    destroy(): void;
+}
 
 /**
  * 初始化 opt-ui selector-sdk。
@@ -34,7 +43,7 @@ export declare function extractElementContext(el: Element): ElementContext;
  * 握手时序说明：SDK 初始化时主动向 parent 发送 opt-ui:ready，Shell 收到后
  * 回复 ping，SDK 回复 pong 并标记 sandboxMode=true，彻底避免时序竞争。
  */
-export declare function init(options?: SelectorOptions): void;
+export declare function init(options?: SelectorOptions): ElementSelectorController;
 
 declare type MouseLeaveCallback = () => void;
 
@@ -66,7 +75,8 @@ export declare type SandboxMessage = {
 
 export declare interface SelectorOptions {
     activationKey?: 'alt' | 'meta' | 'ctrl' | 'shift';
-    onSelect?: (ctx: ElementContext) => void;
+    onSelect?: (ctx: ElementContext, element: Element) => void;
+    isSelectable?: (element: Element) => boolean;
     silent?: boolean;
 }
 
diff --git a/dist/core.es.js b/dist/core.es.js
index 52062d3daf0adb2057d453cdd21ac1c84c5d130d..f19cbfb0b08d71b6b837b3c8729c14c93dac788c 100644
--- a/dist/core.es.js
+++ b/dist/core.es.js
@@ -230,6 +230,21 @@ function extractElementContext(el) {
     boundingRect: { top: rect.top, left: rect.left, width: rect.width, height: rect.height }
   };
 }
+let finderModule = null;
+async function loadFinder() {
+  if (!finderModule) {
+    finderModule = await import("./finder-B-20so_c.js");
+  }
+  return finderModule;
+}
+async function generateCssSelector(el) {
+  try {
+    const { finder } = await loadFinder();
+    return finder(el, { root: document.documentElement });
+  } catch {
+    return void 0;
+  }
+}
 const IGNORED_TAGS = /* @__PURE__ */ new Set(["HTML", "BODY", "HEAD", "SCRIPT", "STYLE", "LINK", "META"]);
 class Selector {
   constructor(options, transport) {
@@ -273,7 +288,7 @@ class Selector {
       if (this.transport.isSandboxMode) {
         this.transport.sendElementSelected(ctx);
       } else if (!this.options.silent) {
-        this.options.onSelect(ctx);
+        this.options.onSelect(ctx, el);
       }
     };
     this.onKeyDown = (e) => {
@@ -302,7 +317,8 @@ class Selector {
       activationKey: options.activationKey ?? "alt",
       onSelect: options.onSelect ?? (() => {
       }),
-      silent: options.silent ?? false
+      silent: options.silent ?? false,
+      isSelectable: options.isSelectable ?? (() => true)
     };
     if (this.options.activationKey === "none") {
       this.alwaysOn = true;
@@ -363,14 +379,19 @@ class Selector {
     this.alwaysOn = active;
     if (!active) {
       this.hovered = null;
-      this.selected = null;
-      this.overlay.hide();
-      this.transport.sendDeselect();
+      if (!this.selected)
+        this.overlay.hide();
     }
   }
+  clearSelected() {
+    this.hovered = null;
+    this.selected = null;
+    this.overlay.hide();
+    this.transport.sendDeselect();
+  }
   getTarget(e) {
     const el = e.target;
-    if (!el || IGNORED_TAGS.has(el.tagName))
+    if (!el || IGNORED_TAGS.has(el.tagName) || !this.options.isSelectable(el))
       return null;
     return el;
   }
@@ -460,12 +481,18 @@ class Transport {
 }
 let instance = null;
 function init(options = {}) {
-  if (instance)
-    return;
-  const transport = new Transport();
-  const selector = new Selector(options, transport);
-  instance = { selector, transport };
-  selector.init();
+  if (!instance) {
+    const transport = new Transport();
+    const selector = new Selector(options, transport);
+    instance = { selector, transport };
+    selector.init();
+  }
+  return {
+    activate: () => instance == null ? void 0 : instance.selector.setActive(true),
+    deactivate: () => instance == null ? void 0 : instance.selector.setActive(false),
+    clearSelected: () => instance == null ? void 0 : instance.selector.clearSelected(),
+    destroy
+  };
 }
 function destroy() {
   if (!instance)
@@ -478,6 +505,7 @@ export {
   Transport,
   destroy,
   extractElementContext,
+  generateCssSelector,
   init
 };
 //# sourceMappingURL=core.es.js.map
