---
title: Components Overview
---

## When To Use

- Use this overview only to discover available components at a high level.
- For implementation work, prefer the component table in `SKILL.md`, then open the specific component `docs.md` and its `demo/` files.
- Do not treat this overview as an API source; individual component docs are authoritative.
