# Status

## Description (en-US)

Add status to Cascader with `status`, which could be `error` or `warning`.

## Source

```vue
<template>
  <sue-space vertical>
    <sue-cascader status="error" placeholder="Error" />
    <sue-cascader status="warning" multiple placeholder="Warning multiple" />
  </sue-space>
</template>
```
