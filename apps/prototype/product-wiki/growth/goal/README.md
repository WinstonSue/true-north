# 目标管理

桌面端当前入口为目标树和目标脑图；目标详情以内嵌概览、子目标、关联任务三 Tab 展示，并支持 AI 拆解、受控状态操作和删除影响检查。独立目标健康面板已废弃，结构摘要由目标详情和工作台承载。

<!-- product-wiki:managed:start -->
## 产品规格（受管）

- 标识：`growth.goal`
- 类型：module
- 产品状态：路线图
- 原型覆盖：完整覆盖
- 原型入口：`/goals`
- 产品定位：提供树形层级的长期目标规划能力，是执行实体的约束源。

### 产品对象

| 对象 | 产品状态 | 原型覆盖 |
| --- | --- | --- |
| 目标 | 路线图 | 完整覆盖 |

### 字段与枚举

| 实体 | 字段 | 类型 | 必填 | 可选值 | 产品状态 | 原型覆盖 | 说明 |
| --- | --- | --- | --- | --- | --- | --- |
| 目标 | `type` | enum | 是 | vision / result | 路线图 | 完整覆盖 | 目标的规划类型。 |
|  | `status` | enum | 是 | todo / doing / done / abandoned | 路线图 | 完整覆盖 | 目标生命周期状态。 |
|  | `importance` | number | 是 |  | 路线图 | 完整覆盖 | 1 至 5 的优先级。 |
|  | `difficulty` | number | 是 |  | 路线图 | 完整覆盖 | 1 至 5 的执行难度。 |
|  | `parentId` | reference | 否 |  | 路线图 | 完整覆盖 | 支持无限层级的目标树。 |

### 视图矩阵

| 视图 | 原型页 | 场景 | 产品状态 | 原型覆盖 | 产品引用 |
| --- | --- | --- | --- | --- | --- |
| 目标树 | `goal` | 构建和浏览目标层级 | 路线图 | 部分覆盖 | `growth.goal.view.tree` |
| 目标详情 | `goal` | 查看和编辑单个目标 | 路线图 | 完整覆盖 | `growth.goal.view.detail` |
| 目标脑图 | `goal` | 理解目标与任务关系 | 路线图 | 部分覆盖 | `growth.goal.view.mindmap` |
| 目标健康面板 | `goal` | 查看结构健康与指标趋势 | 已废弃 | 未覆盖 | `growth.goal.view.dashboard` |

### 规则索引

| 规则 | 实体 | 说明 | 产品状态 | 原型覆盖 | 产品引用 |
| --- | --- | --- | --- | --- | --- |
| 时间范围 | goal | 目标的计划时间范围由开始和结束日期确定，并约束层级排期。 | 路线图 | 完整覆盖 | `growth.goal.rule.time-frame` |
| 类型规则 | goal | 目标仅分为规划和指标，并受直接父子层级约束。 | 路线图 | 完整覆盖 | `growth.goal.rule.type` |
| 优先级继承 | goal | 子目标的重要度和难度不得超过父目标。 | 路线图 | 完整覆盖 | `growth.goal.rule.priority-inheritance` |

<!-- product-wiki:managed:end -->


<!-- product-ref: growth.goal.overview -->
## 目标与价值

目标管理用于表达希望达成的方向、完成期限和重要程度。它让长期愿景可以分解为可检查的阶段成果，并成为任务、待办和习惯的共同上下文。

<!-- product-ref: growth.goal.structure -->
## 业务流程

用户先创建顶层目标，再在必要时建立子目标，补充时间范围与优先级。后续行动关联目标后，用户可从目标回看投入和完成情况。目标被暂停或放弃时，历史关联保留以支持复盘。

<!-- product-ref: growth.goal.view.tree -->
### 目标树

目标树用于浏览父子层级、切换当前上下文并快速进入编辑。它支持按名称、状态、类型和计划时间范围组合筛选，并保留命中目标的父级路径；适合在规划或周度回顾时检查方向是否完整。

<!-- product-ref: growth.goal.view.detail -->
### 目标详情

详情聚焦单个目标的描述、期限、优先级和关联行动。状态仅以 Tag 展示；右上操作区提供完成、编辑、删除与放弃。关联任务可直接打开任务详情抽屉，完成操作会提示关联影响并提供撤销入口。

<!-- product-ref: growth.goal.view.mindmap -->
### 目标脑图

目标脑图以关系图方式呈现目标、子目标和已关联任务，帮助用户发现拆解缺口或重复投入。原型提供结构示意，正式产品需要支持更完整的导航与编辑。

<!-- product-ref: growth.goal.view.dashboard -->
### 目标健康面板

健康面板计划聚合期限风险、近期行动和习惯贡献，用于识别需要干预的目标。该能力已纳入路线图，尚无原型实现。

<!-- product-ref: growth.goal.interaction -->
## 交互语义

创建和编辑应在当前目标上下文中完成。用户删除或暂停目标前需要看到受影响的关联行动；系统应保留历史记录，避免让复盘链路断裂。

<!-- product-ref: growth.goal.rule.time-frame -->
### 时间范围

每个目标都以“开始日期”和“结束日期”定义计划时间范围，边界均包含在范围内。它用于表达目标的期限、在目标树和详情中呈现排期，并为层级拆解提供时间边界；结束日期不能早于开始日期。

顶层目标没有上级时间约束。创建或编辑子目标时，开始日期必须不早于父目标的开始日期，结束日期必须不晚于父目标的结束日期。选择或变更父级目标后，系统按新父目标的计划时间范围重新判断；不满足约束时禁止保存，并提示用户调整子目标的日期或父级目标。

调整父目标计划时间范围时，所有直接子目标仍须落在调整后的范围内。若调整会造成任一子目标越界，用户需要先处理这些冲突后再保存父目标。

原型同时校验父目标改期后的直接子目标范围；存在越界子目标时禁止保存并提示先处理冲突。

<!-- product-ref: growth.goal.rule.type -->
### 类型规则

目标类型仅有规划和指标。无父目标的顶层目标必须为规划；规划目标可以拆分为规划或指标子目标；指标目标只能拆分为指标子目标。规则只检查直接父子关系。

编辑目标时，选择指标父目标会自动将子目标类型改为指标，并只保留指标选项；清除父级目标会自动改为规划。将已有子目标的父目标改为指标前，必须先将其所有直接子目标调整为指标，否则不能保存。

<!-- product-ref: growth.goal.rule.priority-inheritance -->
### 优先级继承

子目标的重要度与难度不能高于父目标。调整父目标的对应值时，系统会检查所有直接子目标，存在冲突时禁止保存。

## 产品边界

工程实现资料见 [目标管理 TechnicalWiki](../../../../../doc/TechnicalWiki/growth/goal.md)。
