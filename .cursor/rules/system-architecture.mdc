---
description: 
globs: 
alwaysApply: true
---

# True North 系统架构规则

## 🏗️ 项目概述

True North 是一个基于 **Monorepo** 架构的全栈生活管理工具套件,采用现代化技术栈构建,旨在提供个人成长、任务管理、习惯追踪、财务管理等功能的一站式解决方案。

## 📁 项目结构

### 根目录架构

```
true-north/
├── apps/                    # 应用层
│   ├── server/             # NestJS 后端服务
│   ├── web/                # React Web 应用
│   └── desktop/            # Electron 桌面应用
├── packages/               # 共享包
│   ├── business/           # 业务域包（前后端共享的业务定义与实现）
│   │   ├── api/            # API 接口/DTO/类型定义
│   │   ├── server/         # 业务核心（实体/仓储/服务/映射）
│   │   ├── web/            # Web 业务 UI（页面/组件/路由）
│   │   ├── vo/             # 值对象
│   │   └── enum/           # 枚举
│   ├── common/             # 通用工具
│   ├── common-web/         # Web 通用组件
│   └── components/         # 可复用组件
├── doc/                    # 文档目录
│   ├── growth/             # 个人成长系统文档
│   │   ├── PRD.md          # 产品需求文档
│   │   ├── TDD.md          # 技术设计文档
│   │   ├── README.md       # 文档导航
│   │   ├── goal/           # 目标管理模块文档
│   │   ├── task/           # 任务管理模块文档
│   │   ├── todo/           # 待办管理模块文档
│   │   └── habit/          # 习惯管理模块文档
│   └── ...                 # 其他系统文档
├── .cursor/                # Cursor IDE 配置
│   └── rules/              # 代码生成规则
├── .windsurf/              # Windsurf 配置
│   └── rules/              # 系统架构规则
└── 配置文件...
```

## 📚 文档规范

### 文档命名规范

> [!IMPORTANT]
> 所有文档必须遵循以下命名规范

- **PRD.md** - Product Requirements Document (产品需求文档)
- **TDD.md** - Technical Design Document (技术设计文档)
- **README.md** - 功能说明 / 导航文档

### 文档层次结构

#### 系统级文档
```
doc/{system}/
├── README.md       # 文档导航
├── PRD.md          # 系统级产品需求文档
└── TDD.md          # 系统级技术设计文档
```

#### 模块级文档
```
doc/{system}/{module}/
├── README.md       # 模块功能说明(精简版)
├── PRD.md          # 模块产品需求文档
└── TDD.md          # 模块技术设计文档
```

### 文档内容分工

| 文档类型 | 主要内容 | 目标读者 |
|---------|---------|----------|
| **PRD.md** | 功能需求、业务规则、用户界面设计、交互流程 | 产品经理、设计师、测试人员 |
| **TDD.md** | 技术架构、数据模型、API设计、枚举定义、实现细节 | 开发者、架构师 |
| **README.md** | 快速概览、核心功能、文档导航 | 所有角色 |

### 个人成长系统文档示例

```
doc/growth/
├── README.md           # 文档导航
├── PRD.md              # 系统级PRD(概述、业务规则、路由结构)
├── TDD.md              # 系统级TDD(技术栈、数据模型、枚举定义)
│
├── goal/               # 目标管理模块
│   ├── README.md       # 功能说明(精简版,引用PRD和TDD)
│   ├── PRD.md          # 完整的功能需求和UI设计
│   └── TDD.md          # 技术实现细节(数据模型、约束实现、API)
│
├── task/               # 任务管理模块
│   ├── README.md
│   ├── PRD.md
│   └── TDD.md
│
├── todo/               # 待办管理模块
│   ├── README.md
│   ├── PRD.md
│   └── TDD.md
│
└── habit/              # 习惯管理模块
    ├── README.md
    ├── PRD.md
    └── TDD.md
```

## 🎯 技术栈

### 后端技术栈 (apps/server)

- **框架**: NestJS (Node.js)
- **数据库**: TypeORM + MySQL/SQLite
- **认证**: JWT + bcrypt
- **调度**: @nestjs/schedule
- **AI集成**: OpenAI API
- **文件处理**: xlsx, ical-generator
- **开发工具**: TypeScript, Jest

### 前端技术栈 (apps/web)

- **框架**: React 18 + TypeScript
- **构建工具**: Vite
- **UI库**: Arco Design + Ant Design
- **状态管理**: React Context + Hooks (个人成长系统)
- **路由**: React Router v6
- **图表**: ECharts (数据统计)
- **样式**: CSS Modules + Less + TailwindCSS
- **工具库**: lodash-es, dayjs, date-fns
- **表单**: Arco Design Form 组件

### 桌面应用 (apps/desktop)

- **框架**: Electron
- **构建工具**: electron-vite
- **打包**: electron-builder
- **开发语言**: TypeScript

## 🏛️ 系统架构

### 分层架构

```
┌─────────────────────────────────────┐
│           前端应用层                 │
│  ┌─────────────┐  ┌─────────────┐   │
│  │  Web App    │  │  Desktop    │   │
│  │  (React)    │  │ (Electron)  │   │
│  └─────────────┘  └─────────────┘   │
└─────────────────────────────────────┘
┌─────────────────────────────────────┐
│           共享包层                   │
│  ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐   │
│  │ VO  │ │Enum │ │组件 │ │工具 │   │
│  └─────┘ └─────┘ └─────┘ └─────┘   │
└─────────────────────────────────────┘
┌─────────────────────────────────────┐
│           后端服务层                 │
│  ┌─────────────────────────────────┐ │
│  │        NestJS Server           │ │
│  │  ┌─────┐ ┌─────┐ ┌─────┐      │ │
│  │  │业务 │ │认证 │ │AI   │      │ │
│  │  │模块 │ │模块 │ │模块 │      │ │
│  │  └─────┘ └─────┘ └─────┘      │ │
│  └─────────────────────────────────┘ │
└─────────────────────────────────────┘
┌─────────────────────────────────────┐
│           数据存储层                 │
│  ┌─────────────┐  ┌─────────────┐   │
│  │   MySQL     │  │   SQLite    │   │
│  │  (生产环境)  │  │  (开发环境)  │   │
│  └─────────────┘  └─────────────┘   │
└─────────────────────────────────────┘
```

## 🔧 业务模块架构

### 个人成长系统 (Growth)

个人成长系统采用**四级联动**架构:

```
目标(Goal) → 任务(Task) → 待办(Todo)
     ↓
   习惯(Habit)
```

#### 核心模块

1. **目标管理 (Goal)**
   - 树形层级结构,支持无限层级
   - 懒加载优化性能
   - 父子约束机制(时间、重要程度)

2. **任务管理 (Task)**
   - 二选一关联规则(父任务 OR 目标)
   - 时间跟踪功能
   - 继承父级约束规则

3. **待办管理 (Todo)**
   - 四象限优先级管理
   - 5种视图(今日/本周/日历/全部/统计)
   - 批量操作支持

4. **习惯管理 (Habit)**
   - **强制关联目标**(必须至少一个)
   - 连续天数跟踪
   - 完成率统计

#### 业务规则

**关联规则**:
| 实体 | 父级关联 | 约束 |
|------|---------|------|
| 目标 | 可选关联父目标 | - |
| 任务 | 二选一: 父任务或目标 | isSubTask ? parentId : goalId |
| 待办 | 可选关联任务或目标 | - |
| 习惯 | 必须关联至少一个目标 | goals.length >= 1 |

**时间约束规则**:
```
目标时间范围
    ├─ 任务时间范围 ⊆ 目标时间范围
    │   └─ 子任务时间范围 ⊆ 父任务时间范围
    └─ 待办时间 ⊆ 目标时间范围 (如果关联)
```

**重要程度约束规则**:
```
目标重要程度 >= 任务重要程度 >= 子任务重要程度
目标重要程度 >= 待办重要程度 (如果关联)
```

### 后端核心域实现 (packages/business/server/src)

```
business/
├── growth/                 # 个人成长模块
│   ├── todo/
│   ├── task/
│   ├── goal/
│   ├── habit/
│   └── track-time/
├── expenses/              # 财务管理模块
├── users/                 # 用户管理模块
├── auth/                  # 认证授权模块
├── ai/                    # AI 服务模块
├── calendar/              # 日历模块
└── excel/                 # Excel 处理模块
```

### 前端页面模块 (packages/business/web/src/pages)

```
pages/
├── growth/                # 个人成长页面
│   ├── goal/             # 目标管理
│   ├── task/             # 任务管理
│   ├── todo/             # 待办管理
│   ├── habit/            # 习惯管理
│   └── components/       # 共享组件
├── expense/               # 财务管理页面
├── ai/                    # AI 助手页面
├── timer/                 # 计时器页面
├── dashboard/             # 仪表板页面
├── user/                  # 用户中心页面
├── login/                 # 登录页面
└── ...                    # 其他功能页面
```

### 前端状态管理模式

个人成长系统使用自定义的 `createInjectState` 工具:

```typescript
// 示例: Goal Context
export const [GoalProvider, useGoalContext] = createInjectState<{
  PropsType: {
    children: React.ReactNode;
  };
  ContextType: {
    loading: boolean;
    goalTree: GoalVo[];
    selectedGoal: GoalVo | null;
    fetchGoalTree: () => Promise<void>;
    // ...
  };
}>(() => {
  // 状态实现
});
```

**特点**:
- 每个模块独立的 Context Provider
- 通过 custom hooks 访问状态
- 类型安全

## 📦 包管理架构

### Monorepo 管理

- **包管理器**: pnpm (workspace)
- **构建工具**: Turbo
- **版本管理**: workspace:* 内部依赖

### 共享包设计

```
packages/
├── business/              # 业务域包
│   ├── api/               # API 接口/DTO/类型定义
│   ├── server/            # 业务核心（实体/仓储/服务/映射）
│   ├── web/               # Web 业务 UI（页面/组件/路由）
│   ├── vo/                # 值对象 (Value Objects)
│   └── enum/              # 业务枚举
├── common/                # 通用工具
├── common-web/            # Web 通用组件
└── components/            # 可复用组件
```

## 🔄 数据流架构

### 标准业务模块结构

后端核心域结构 (packages/business/server/src/{module})

```
{module}/
├── entities/              # 数据实体 (TypeORM)
├── dto/                   # DTO 类包含内置映射方法
├── {module}.repository.ts # 仓储
└── {module}.service.ts    # 业务服务
```

后端适配层结构 (apps/server/src/business/{module})

```
{module}/
├── {module}.controller.ts # 控制器
└── {module}.module.ts     # 模块定义
```

### 数据流向

```
Client Request
    ↓
Controller (验证 + 路由)
    ↓
Service (业务逻辑)
    ↓
Repository (数据访问)
    ↓
Database (数据存储)
    ↓
Entity → DTO → VO
    ↓
Client Response
```

## 🛠️ 开发工具链

### 构建和部署

- **开发服务**: `pnpm dev` (Turbo 并行启动)
- **构建**: `pnpm build` (Turbo 优化构建)
- **部署**: 独立的部署脚本
- **代码质量**: ESLint + Prettier + Husky

### 开发环境

- **IDE**: Cursor / Windsurf (AI 辅助开发)
- **调试**: NestJS Debug + React DevTools
- **测试**: Jest (单元测试 + 集成测试)
- **API文档**: 自动生成 (计划中)

## 🔐 安全架构

### 认证授权

- **JWT Token**: 无状态认证
- **密码加密**: bcrypt 哈希
- **权限控制**: 基于角色的访问控制 (计划中)

### 数据安全

- **输入验证**: class-validator
- **SQL注入防护**: TypeORM 参数化查询
- **XSS防护**: 前端输入过滤

## 📊 数据架构

### 数据库设计原则

- **实体关系**: TypeORM 装饰器定义
- **字段类型**: 标准化字段类型映射
- **索引策略**: 基于查询模式优化
- **数据迁移**: TypeORM Migration

### 数据模型规范

```typescript
// 实体定义示例
@Entity()
export class TodoEntity {
  @PrimaryGeneratedColumn()
  id: number;

  @Column('varchar')
  @IsString()
  title: string;

  @Column('boolean', { default: false })
  @IsBoolean()
  completed: boolean;

  @CreateDateColumn()
  createdAt: Date;
}
```

## 🚀 性能优化

### 前端优化

- **代码分割**: Vite 动态导入
- **组件懒加载**: React.lazy + Suspense
- **状态管理**: 精细化状态更新
- **缓存策略**: Context 缓存数据
- **懒加载**: 目标树按需加载子节点
- **防抖/节流**: 搜索输入防抖、滚动加载节流

### 后端优化

- **数据库优化**: 索引 + 查询优化
- **缓存策略**: 内存缓存 (计划中)
- **异步处理**: 队列 + 调度任务
- **API优化**: 分页 + 字段筛选

## 📋 开发规范

### 代码规范

- **命名约定**: camelCase + PascalCase
- **文件组织**: 功能模块化组织
- **注释规范**: JSDoc + 业务注释
- **类型安全**: 严格 TypeScript 模式

### 文档规范

- **文档命名**: PRD.md / TDD.md / README.md
- **文档分层**: 系统级 + 模块级
- **内容分工**: 产品需求 vs 技术设计
- **及时维护**: 代码变更时同步更新文档

### Git 工作流

- **分支策略**: Feature Branch + Main
- **提交规范**: Conventional Commits
- **代码审查**: Pull Request 流程
- **自动化**: Husky 预提交检查

---

**维护者**: True North Team  
**最后更新**: 2025-12-22
